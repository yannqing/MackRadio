version: '3.8'
services:

  macksimpleai:
    container_name: macksimpleai
    build:
      context: .
      dockerfile: ./Dockerfile.dockerfile
    ports:
      - "8080:8080"
    expose:
      - 8080
    networks:
      - mynetwork


networks:
  mynetwork:
    external: true


