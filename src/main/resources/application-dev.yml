spring:
  application:
    name: MackRadio
  datasource:
    hikari:
      max-lifetime: 1800000
      connection-timeout: 30000
      idle-timeout: 1200000
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://mysql:3306/video
    username: yannqing
    password: wyjh030208..
  data:
    redis:
      database: 0
      host: mackRedis
      jedis:
        pool:
          enabled: true
          max-active: 8
          max-wait: 1
          max-idle: 8
          min-idle: 0
      connect-timeout: 100000
      password: wyjh030208..
server:
  port: 8080
  servlet:
    session:
      cookie:
        domain: localhost
        http-only: false
      timeout: 60m
  tomcat:
    connection-timeout: 60000
    threads:
      max: 200
